<h1>Raging in the Streets!</h1>

{{1+3}}

{{words.messedUp}}
{{words.sayHello()}}



<form ng-submit="words.addWord(newWord)">
	<input type="text" ng-model="newWord.name" placeholder="word">
	<input type="text" ng-model="newWord.description" placeholder="description">
	<button>Seguro!</button>
</form>

<div ng-repeat="name in WordFactory">
	{{word.name}} {{ word.description}}
</div>

