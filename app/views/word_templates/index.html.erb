<h1>Raging in the Streets!</h1>

{{1+3}}

{{words.messedUp}}
{{words.sayHello()}}

<form ng-submit="words.addWord(newWord)">
	<input type="text" ng-model="newWord.name">
	<button>Basura!</button>
</form>
